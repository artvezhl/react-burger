import React from "react";

import popupStyles from './popup.module.css'

export default class IdPopup extends React.Component {
    render() {
        return (
            <div className={ popupStyles.popupBackground }></div>
        )
    }
}